# DeepRAG 依赖（由 pyproject.toml 导出的安装入口）
# 推荐：pip install -e ".[ui,llm,dev]"
# 最小可跑（offline 规则 + 向量库）：

# runtime
langgraph>=0.2.0
chromadb>=0.5.0
rank-bm25>=0.2.2
jieba>=0.42.1
python-dotenv>=1.1.0

# UI
streamlit>=1.40.0
plotly>=5.0  # app.py 中的图表（懒加载）

# LLM 链路（可选后端仍依赖各自 API Key / Ollama）
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0
langchain-ollama>=0.2.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# API 生产入口
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
httpx>=0.27.0
psutil>=5.9.0

# 可选（按需取消注释）
# qdrant-client>=1.12.0
# sentence-transformers>=3.0.0
# psycopg2-binary>=2.9.0
# pgvector>=0.2.0

# dev（格式化 / 静态检查 / 测试）
pytest>=8.0
pytest-asyncio>=0.25.0
pytest-cov>=5.0
pytest-html>=4.0
black
isort
ruff
mypy
pre-commit

# observability（src/observability，按需启用）
langfuse
tiktoken
