# 工作日志 2026-07-18 — 建库完成 + 免费模型切换

## 1. 向量库构建（已完成）

- 结束时间：约 `2026-07-18T12:21:29`
- 耗时：`4561s`（~76 分钟，desktop 档）
- 模式：Qdrant **server** + 同名去重 + 书籍纳入
- 结果：`ok=10/10`

| collection | points | 说明 |
|------------|-------:|------|
| proj_work | 38381 | 已有 skip |
| proj_thesis | 70182 | 已有 skip |
| proj_psychology | 4173 | 已有 skip |
| proj_social | 139942 | 已有 skip |
| **proj_zhesi** | **142269** | **含书籍 72 本** |
| proj_ideas | 50048 | 去重后 ~9.8k 文件 |
| proj_assistant | 51994 | |
| proj_tools | 380 | |
| proj_worklog | 6 | |
| proj_api_router | 72 | |

注册表：`哲思灵智/向量库注册表_qdrant.json`

### 建库优化点
- 同名文件只保留 1 份（优先 `书籍/`、更大文件）
- ideas 从约 28 万文件压到约 1 万
- 书籍单文件上限 48MB
- 排除 site-packages / node_modules / trae 聊天等垃圾目录

## 2. LLM 默认与降级（已改）

| 项 | 值 |
|----|-----|
| 默认后端 | `cerebras` |
| 默认模型 | `gpt-oss-120b` |
| auto 顺序 | Cerebras → Groq → Silicon → Zhipu … |
| fallback 链 | Cerebras → Groq 8B → Silicon GLM-Z1 → Zhipu Flash → Ollama |
| 冒烟 | Cerebras 854ms 返回正常 |

前端 `app.py` 已增加 Cerebras / Groq 选项。

## 3. 免费模型评测

见 `docs/free_model_benchmark_report_v3.md`：
1. Cerebras GPT-OSS-120B 8.82 / 805ms  
2. Groq Llama3.1-8B 8.64 / 1071ms  
3. Zhipu / Silicon 质量可但慢  

## 4. 多学科评测

题库 197 题 × 细化分支，脚本支持本地无依据时 DuckDuckGo+LLM 回退。  
（本会话启动全量跑分见 `哲思灵智/multidim_eval_run.log`）
