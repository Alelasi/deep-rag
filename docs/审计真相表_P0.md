# DeepRAG 审计真相表 P0（2026-07-16）

原则：**代码 / 文档 / 评测三方说同一种真话**。处理策略：改代码 | 降调文档 | 删除声明。

| ID | 问题 | 证据 | 处理 |
|----|------|------|------|
| P0-1 | Self-RAG regenerate 曾为死路径 | `route_after_fact_check` 恒返回 `check_conflicts` | **已改代码**：`ENABLE_SELF_RAG_LOOP`（默认 false）可开环 |
| P0-2 | reliability 空壳 | `src/reliability/` 无业务 py | **降调**：仅 `README.md` 占位说明 |
| P0-3 | security 虚标 | 无 `src/security/` | **删除声明**：README 去掉 security 树；不伪造模块 |
| P0-4 | 入口/依赖/版本乱 | 旧 README 写 demo_app_final / 无 requirements；pyproject 0.2.0 | **已对齐**：`app.py`、`requirements.txt`、`0.2.9` / 能力版本 2.9.x |
| P0-5 | 双 Hybrid / 双 MCP | hybrid vs hybrid_retriever；两套 MCP 协议 | **收敛**：主 Hybrid=`hybrid.HybridRetriever(indexer)`；并行=`ParallelHybridRetriever`；MCP 主入口 `start_mcp_server.py` |
| P0-6 | 默认 offline 节点 | graph 默认 `*_offline` | **诚实默认 + 开关**：`USE_LLM_PIPELINE_NODES`（默认 false） |
| P0-7 | Web mock / 空证据 | mock 可当证据生成 | **已改**：mock 不入生成证据；`no_knowledge` 拒答 |
| P0-8 | 评测口径冲突 | golden 60≠150；覆盖率≠100%；意图 L1≈72% | **降调文档** + e2e_20 可复现 |
| P0-9 | Answer Relevancy≈0 | 英文 split 中文失效 | **已改**：jieba/bigram 离线启发式 |

## 评测资产实测（冻结）

| 资产 | 实际规模 | 用途 |
|------|----------|------|
| `evaluation/golden_test_set.json` | **60** | 主 golden |
| 意图评测 v5 corrected | **150**（L1≈**72%**） | 仅意图，≠系统 95% |
| 离线 RAGAS 报告 20260521 | 5 题样例 | 历史启发式，非官方 ragas |
| `evaluation/e2e_20.json` | **20** | 主路径 `src.graph` 回归 |

## 版本对照

| 类型 | 值 |
|------|-----|
| 包版本 `pyproject` | 0.2.9 |
| 能力版本 | 2.9.x（UI/注释可能仍写 v2.5–v3.0 局部特性） |
| 默认 Self-RAG 闭环 | **关闭**（质量模式 `ENABLE_SELF_RAG_LOOP=true`） |
