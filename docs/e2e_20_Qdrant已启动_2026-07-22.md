# e2e_20 实测 · Qdrant 已启动（2026-07-22）

## 环境

| 项 | 值 |
|----|-----|
| Docker | Desktop 4.38 · Engine 27.5.1 |
| 容器 | **`zhesi-qdrant`**（`qdrant/qdrant:v1.12.0`） |
| 端口 | `0.0.0.0:6333-6334` |
| 健康 | `GET /readyz` → `all shards are ready`（**curl 需 `--noproxy '*'`**，否则走 7890 代理 502） |
| 集合 | proj_psychology / work / thesis / zhesi / social … 共 10 个 |
| VECTOR_DB | qdrant · host `127.0.0.1:6333` |

## 结果

| 项 | 值 |
|----|-----|
| 报告 | `evaluation/reports/e2e_20_20260722_qdrant_up.json` / `.md` |
| **命中** | **19/20（95.0%）** |
| no_knowledge 率 | 5%（e2e_17 拒识题） |
| mock_web 率 | 0% |
| 失败题 | **e2e_08**「恐贪指数算法大致怎么计算？」答案空 / 关键词未命中（约 60s） |

> 报告自注：**启发式关键词命中，非官方 RAGAS；勿直接写成简历 95% SLA。**  
> 面试可说：「向量服务起来后主路径 e2e_20 当次 **19/20**；失败 1 题在恐贪算法细节，可复盘检索域/生成。」

## 对照

| 时刻 | 条件 | 得分 |
|------|------|------|
| 下午阻塞 | Qdrant 未起 | **1/20**（仅拒识） |
| 现已修复 | zhesi-qdrant Up | **19/20** |

## 运维备忘

```bash
# 启动（已有数据容器，优先这个，不要另起抢 6333 的旧 qdrant）
"/c/Program Files/Docker/Docker/resources/bin/docker.exe" start zhesi-qdrant

# 健康（绕过本机 HTTP 代理）
curl --noproxy '*' http://127.0.0.1:6333/readyz
curl --noproxy '*' http://127.0.0.1:6333/collections

# 重跑 e2e
cd deep-rag
.venv/Scripts/python.exe evaluation/run_e2e_20.py --out evaluation/reports/e2e_20_rerun.json
```

**勿**对已有知识库路径用 Chroma PersistentClient 直连。

## 后续可选

1. 查 e2e_08：恐贪算法文档是否在可路由集合、生成是否被超时/拒答  
2. 面试话术/简历：用「当次 19/20」替换阻塞口径  
3. 关 Docker 前如需保留服务：`docker update --restart unless-stopped zhesi-qdrant`（可选）

**文件**：`deep-rag/docs/e2e_20_Qdrant已启动_2026-07-22.md`
